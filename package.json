{
  "name": "pycortex-roidraw",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "description": "Pycortex WebGL viewer with in-browser ROI drawing + editing + export. Modular: pure core / viewer adapter / UI.",
  "license": "BSD-2-Clause",
  "scripts": {
    "build": "node build.mjs",
    "test": "npm run test:js && npm run test:py",
    "pretest:js": "npm run build",
    "test:js": "node --test test/*.test.js",
    "test:py": "python3 test/test_bake.py && python3 test/test_sulci_svg.py"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
