{
  "name": "pycortex-roidraw",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "description": "In-browser ROI + sulcus drawing, editing, and export for pycortex WebGL viewers. Modular: pure core / viewer adapter / UI.",
  "license": "BSD-2-Clause",
  "scripts": {
    "build": "node build.mjs",
    "lint": "eslint .",
    "test": "npm run lint && npm run test:js && npm run test:py",
    "pretest:js": "npm run build",
    "test:js": "node --test test/*.test.js",
    "test:py": "python3 -m unittest discover -s test -p 'test_*.py'"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.24.0",
    "eslint": "^10.8.1",
    "globals": "^17.11.0"
  }
}
